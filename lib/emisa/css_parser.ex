defmodule Emisa.CssParser do
  @doc """
  Parses a CSS string.

      [
        {:charset, "utf-8", []},
        {:declaration, "a", [{"color", "blue"}]},
        {:media, "(min-width: 780px)", [ ... ]}
      ]
  """

  def parse(src) do

  end
end
